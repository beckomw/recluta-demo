/**
 * Hooks Index
 *
 * Central export point for all custom React hooks.
 *
 * @example
 * import { useResumes, useJobs, useLocalStorage } from '../hooks';
 */

export {
  useLocalStorage,
  useResumes,
  useJobs,
  useApplications,
} from './useLocalStorage';
