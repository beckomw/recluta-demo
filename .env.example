# WebLLM Configuration

# Enable debug mode for detailed logging and performance tracking
# Set to 'true' to enable verbose logging in console
# Default: false (only errors and warnings are logged)
VITE_WEBLLM_DEBUG=false

# Example usage:
# Development: VITE_WEBLLM_DEBUG=true npm run dev
# Production: VITE_WEBLLM_DEBUG=false npm run build
